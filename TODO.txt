bakedItem:texture(texture, u, v, width, height, scale) -- Sets the texture of this item
bakedItem:applyPose(pose) -- Applies a preset pose for all display modes
bakedItem:rot(mode, rotation)
bakedItem:pos(mode, position)
bakedItem:scale(mode, scale)

-- Make bakery and baked item model injestion
-- Add modes helper
-- Asserts everywhere
-- Make methods that aren't meant for a particular skull type not cause any issues
-- Get ready for release

-- Work on the wiki